# 2026-04-24

## `_spec/` triad created and promoted to `docs/_spec/`

- **DESIGN.md** — canonical Ocean-light design spec conforming to the [google-labs-code DESIGN.md](https://github.com/google-labs-code/design.md) format (YAML tokens + prose). Covers colors (17 hex), typography (9 styles), spacing, rounded, and 13 component definitions. Authored from `docs/site.css` + `docs/base.css` as source of truth.
- **11 variant siblings** — `DESIGN-{scheme}-{mode}.md` for every scheme × mode combo except Ocean-light (which is `DESIGN.md`). Each ~35 lines: YAML color block + prose notes on non-obvious decisions, each pointing back to `DESIGN.md` for inherited typography / spacing / rounded / components. Source lines from `site.css` cited on every file.
- **LAYOUT.md** — parallel spec describing HTML skeleton, z-index stack (0 particles → 800 toast), positioning model (topnav is `position: relative`, not fixed), sidebar anatomy, section types, grid breakpoints, pin geometry (margin-inline push, not transform), background-image strategy. ~20KB.
- **BEHAVIOR.md** — third leg of the triad. YAML tokens for motion (durations, eases), breakpoints, `base-state` persistence schema, 19 named interactions, accessibility rules, keyboard map. Prose covers initialization phases, component state machines, reduced-motion fallbacks. Captured non-obvious bits like the 320ms carousel wrap-cleanup timer, `preload` class mechanism, and the `0.01ms` reduced-motion trick (not `0`, to preserve `transitionend`).
- **Filesystem layout** — all 14 spec files initially written to `_spec/` at root, then moved to `docs/_spec/` with a root symlink so they're served at `https://dcs.spa/_spec/*.md` and also browsable via the in-page doc viewer (tree-widget branch added with `data-doc-path="_spec/…"` entries, primary specs listed first, 11 variants after in scheme-mode order).

## Tour deck — sidebar-embedded slide presentation

- Added a new **Tour panel** as the first LHS panel (panel index 0, so new visitors see it on sidebar-open). 6-slide mini-carousel nested inside the outer panel carousel, with its own top-bar `◀ 01/06 ▶` controls **independent** of the outer sidebar chevrons/dots.
- **State isolated in `site.js` closure** — Deck does not touch `base-state` localStorage. Keeps `base.js` generic and the widget truly modular. Click delegation on `[data-deck-dir]` / `[data-deck-goto]`, plus `ArrowLeft`/`Right`/`Home`/`End` keyboard when the deck has focus. Wraps both directions.
- **6 decks drafted** — The Problem (wasted-whitespace pill diagram) → The Pattern (chip row showing Tour › Nav › About › Docs) → Mobile-First (code snippet `transform: translateX(0)`) → Desktop (stat card `10–100%`) → Motion (mini animated slide-vs-fade demos) → Persistence (faux `localStorage` stack). Large ghost number "01" watermark per slide, kicker with accent bar, display headline, body copy, figure.
- **Entry stagger** — active slide's children fade + rise with 80ms stagger on each activation. `html.preload` class forces children to their pre-animation position so the stagger plays on **initial paint** too (transitions don't fire for styles applied before first render). Big number uses `cubic-bezier(0.34, 1.56, 0.64, 1)` ease-spring; others use `ease-out`.
- **Files touched:** `docs/index.html` (4th LHS carousel dot + Tour panel block at position 0 with 6 slides), `docs/site.css` (~300-line Deck component using `color-mix(in oklch, ...)` so it adapts to every scheme), `docs/site.js` (`initDeck()` IIFE).

## Dropped `themes/` folder

- Confirmed nothing external consumed `themes/stone.css` (`spe` uses `base.css + site.css` directly, not `themes/stone.css`). Inside the repo: 2 × `CLAUDE.md`, 3 × `README.md`, 1 × `_doc/usage-patterns.md`, 2 × `_spec/*.md` references.
- **Rewrote the three-pattern model to two** — "Documentation Site" pattern now uses the same `base.css + site.css` pair as marketing sites. If the HTML never mounts hero / service-card / pricing / CTA components, those rules simply do not render. No runtime penalty. Removes the only reason `themes/stone.css` existed as a separate file.
- **Updated:** `README.md` (file table, CSS-variable-contract blurb, Pattern 2 example), `CLAUDE.md` (layout tree + Pattern 2 example), `docs/_doc/usage-patterns.md` (intro + Pattern 2), `docs/_spec/DESIGN.md` (Do's bullet), `docs/_spec/DESIGN-stone-light.md` (footer reference).
- **Deleted** `themes/stone.css` and the `themes/` folder entirely.

## Dropped `base.txt`, added `docs/ai.txt`

- `base.txt` (246 lines) was an early plaintext DCS site-generation spec, superseded by the formal `_spec/` triad. Only referenced in one CLAUDE.md layout-tree line.
- **Fetched** `https://aitxt.ing/ai.txt` (162 lines) — the spec format with YAML frontmatter, cascading discovery, distributed context via links ending in `ai.txt`, and the `<link rel="prefetch">` HTML Discovery convention.
- **Authored** `docs/ai.txt` (202 lines) as the new canonical AI-readable spec. Sections: About / Architecture / Features / Capabilities / **Limitations (explicit non-goals)** / Data (structured facts — 23 keys) / Usage Patterns / FOUC / HTML Skeleton / AI Guidelines / Permissions / See Also / Contact. Pulled HTML skeleton, CSS variable contract, and responsive breakpoints from `base.txt`; pulled architecture, color-system, and component facts from `_doc/`; pulled motion/interaction/pin-geometry from `_spec/`.
- **Crucial for AI agents:** the Limitations section is deliberately blunt — "no framework, no build step, no npm package, no component API, no React" — to prevent hallucinated capabilities. The AI Guidelines section tells agents where to look for deeper context (`_spec/` for structured facts, `_doc/` for prose) and when *not* to edit (`base.css` / `base.js` are framework files).
- **Symlinked** `ai.txt → docs/ai.txt` at root (same pattern as `base.css` / `site.css` / `md.js`).
- **Added** `<link rel="prefetch" href="ai.txt">` to `docs/index.html` `<head>` per the aitxt HTML Discovery section — browsers and AI-aware browser extensions pick it up during idle fetch.
- **Deleted** `base.txt`; updated `CLAUDE.md:34` entry.

## ai.txt added to the doc-viewer tree

- Inserted `ai.txt` as the first child of the `docs/` tree-branch (depth 1), **above** `_doc/` and `_spec/`. It now reads as the intro/overview entry before the structured folders. Uses the Lucide `bot` icon to distinguish it from the `file-text` and `file-code` siblings.
- Wired as `data-doc-path="ai.txt"` — the existing `showDoc()` handler fetches any path, so the `.txt` extension is irrelevant. Renders via `md.js` (with frontmatter stripped).

## md.js frontmatter strip

- After the `_spec/` files started appearing in the in-page doc viewer, their YAML frontmatter rendered as a leading paragraph of pseudo-text (md.js has no frontmatter awareness and its `^[-*_]{3,}\s*$` rule treated the `---` lines as `<hr>`).
- Surveyed conventions: **CommonMark / GFM** don't define frontmatter; **GitHub's web UI** renders it as a two-column table (unique); **Jekyll / Hugo / Zola / Astro / MDX / Pandoc** all strip it as metadata. The SSG / doc-tool convention is overwhelmingly "strip".
- Shipped the strip-only approach — 3-line addition at the top of `md()`: `s = s.replace(/^---\r?\n[\s\S]*?\r?\n---\r?\n?/, '')`. Matches Jekyll/Hugo/Pandoc, avoids bringing in a YAML parser (which would violate the zero-deps invariant), and the frontmatter is still visible in the raw `.md` files anyway.

## Section ledes — fleshing out the banner→cards gap

- Mark flagged "missing promotional text" between the **Base + Site Separation** banner and the **6 Color Schemes** banner — said it felt blank and asked me to check git history for removed prose.
- **Git history verdict:** no regression. Structure has been banner → 4-card grid since the initial release (`bdaabb8`); no prose was ever between them. The `bg-image-section` banners carry only `section-header` (tag/title/subtitle) and the following `content-section` jumped straight to `services-grid`. Combined with `min-height: 30vh` banners and `.reveal` cards starting at `opacity: 0`, the visual gap read as "empty" even though the grid was right there.
- **Fix:** authored 5 lede paragraphs (2 sentences each, promotional tone) and inserted one at the top of each `content-section` above its `services-grid`: Features, Architecture (Base + Site), 6 Color Schemes, Components, Usage.
- **New `.section-lede` style** in `site.css` — 720px max-width, centered, `--text-lg`, muted color, `line-height: 1.7`, with `<code>` and `<strong>` accents that adopt scheme colors. Carries the `reveal` class so the stagger stays coherent with the cards below.
- **Files touched:** `docs/site.css` (~18-line `.section-lede` block after `.section-subtitle`), `docs/index.html` (5 × `<p class="section-lede reveal">…</p>`).

## ai.txt discoverability + canonical title/email/copyright sweep

- **Discoverability test** — fetched `https://dcs.spa/` via WebFetch and asked it to report any ai.txt pointer. Result: the `<link rel="prefetch" href="ai.txt">` in `<head>` was stripped during HTML→markdown conversion (as is the rule for most LLM fetchers today), and the tree-widget `<span data-doc-path="ai.txt">` isn't an `<a href>` either, so it also disappeared. Only the aitxt convention of probing `/ai.txt` directly (like `/robots.txt`) would have found it. Direct fetch of `https://dcs.spa/ai.txt` worked fine — the file was never the problem, discovery was.
- **Fix — visible footer link.** Added `<a href="ai.txt">ai.txt (AI context)</a>` with a Lucide `bot` icon in the footer Links column, right under the Documentation row. Survives markdown conversion in every mainstream fetcher, so a basic `WebFetch(https://dcs.spa/)` now surfaces the pointer in rendered content without relying on resource-hint parsing.
- **Canonical title: singular → plural** — "Dual Carousel Sidebar" → "Dual Carousel Sidebars" across live docs. Touched: `CLAUDE.md`, `README.md` (laradcs blurb), `docs/ai.txt` (×3: frontmatter `project`, H1 heading, Data `name`), `docs/_spec/DESIGN.md` (frontmatter `name`), `docs/_spec/BEHAVIOR.md` and `docs/_spec/LAYOUT.md` (frontmatter `description`). Journals left unchanged — dated historical records shouldn't be retconned.
- **Email swap** — `mc@netserva.org` → `mc@dcs.spa` project-wide: all 5 source-file copyright headers (`base.css`, `base.js`, `site.css`, `site.js`, `md.js`), both markdown license sections (`CLAUDE.md`, `README.md`), ai.txt author + Contact line, and the index.html footer (now also a `mailto:` link).
- **Unified copyright format** — "Copyright © 2026 Mark Constable <mc@dcs.spa>" inserted wherever a license reference appears. Per Mark's instruction the year range collapsed from `2015-2026` to just `2026` (loses the historical DCS-predecessor start-year anchor, but matches his canonical form). Places touched: 5 source-file headers, `CLAUDE.md` License, `README.md` License, `docs/ai.txt` Permissions (License bullet), index.html "MIT Licensed" banner subtitle (now reads "Copyright © 2026 Mark Constable <mc@dcs.spa> — use it in any project…"), and the index.html footer-copyright line.
- **Files touched:** 12 files, +24/−20 lines. No content regressions.

## Memory

- `sibling_project_conflation.md` — earlier session had me wrongly concluding a DCS feature "never existed" because I only searched `dcs.spa` git history; Mark pointed out DCS-consuming projects (`motd`, `rentanet`, `spe`, `laradcs`) also carry DCS history. Saved as project memory so future sessions grep all consumers before declaring a memory missing.
