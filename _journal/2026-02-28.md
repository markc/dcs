# 2026-02-28

Retroactively reconstructed from git history — `_journal/` convention came later.

## Session summary

Big polish pass: backported the panel carousel from markweb and fleshed out the Appearance controls. Eight commits across the day.

- **Panel carousel backport** (`8ae4bbd`) — sidebars become full-height (`top:0; height:100vh`) with a multi-panel carousel inside. Hamburger buttons moved to fixed glass-morphism overlays at the top corners. Added chevron nav, coloured dot indicators, and panel titles. Panel index persists in localStorage (`leftPanel`/`rightPanel`). Seamless topnav effect: sidebar inner borders fade in on scroll. Topnav shifts inward when sidebars are pinned on desktop. Also fixed `setScheme()` which was missing `crimson`/`stone` from the class-removal list.
- **Pinned hamburger visibility** (`1ae5c90`) — keep hamburger buttons visible even when sidebars are pinned, so there's always a way to unpin/close without reaching into the sidebar itself.
- **Topnav becomes flow element** (`2e681be`) — switched topnav from `position:fixed` to `position:relative` so it scrolls with the page (matching markweb). Sidebar inner borders now use `::after` pseudo-elements that animate `top` on scroll. Main content no longer needs the `topnav-height` margin offset. Pinned sidebars use `margin` instead of `left`/`right` to offset the topnav correctly.
- **48×48 hamburger tap targets** (`0a16473`) — square up the hamburger buttons for reliable mobile taps.
- **Pin-toggle spacing** (`7f26618`) — added margin on the pin toggle so it doesn't crowd the hamburger icon.
- **Appearance panel** (`b62a2c1`) — renamed the Theme panel to Appearance and built out the full settings UI: Light/Dark toggle buttons (replacing the icon toggle), Slide/Fade carousel mode (500 ms crossfade, persisted), sidebar width slider (200–400 px default 300, persisted), and colour-scheme selectors with coloured dots. Fade mode uses CSS-grid stacking with opacity crossfade.
- **Carousel mode-switch: state before class** (`3667fbd`) — prevents a visible panel flash when toggling Slide/Fade by setting the target mode's state (transform / `.active`) *before* flipping the `fade-mode` class that changes layout mode.
- **Suppress transitions during mode switch** (`cc0f02a`) — kills inline transitions on the track and panels, applies all DOM changes, forces a reflow, then restores transitions — so clicking Slide/Fade produces zero visual movement on either sidebar.

## Notes

- The Slide/Fade switch needed two passes to get right (`3667fbd` then `cc0f02a`) — the first ordering fix wasn't sufficient; transitions also had to be suppressed end-to-end.
