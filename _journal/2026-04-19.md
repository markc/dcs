# 2026-04-19

## Session summary

- **CLAUDE.md refreshed** to match post-Feb-24 state: panel carousel, Appearance panel, tree widget, `--sidebar-width-*` tokens, corrected `~/.gh/dcs.spa` path, dropped stale line counts, removed `div.overlay` from the HTML tree.
- **Sidebar width refactor** — replaced the shared `--sidebar-width: 300px` with independent `--sidebar-width-left` / `--sidebar-width-right` (default 30%, range 25–75%). Two sliders in the Appearance panel. Added `clamp(260px, …, 75vw)` helpers so narrow viewports stay usable. Pinning both sides at 50% now produces a true split-screen (main content collapses to zero).
- **Autonomous sidebars** — `toggleSidebar` no longer closes the opposite non-pinned sidebar. Each side is now independent.
- **Overlay removed entirely** — deleted the `.overlay` element, CSS (`body.sidebar-open .overlay` etc.), and related click handlers. Also dropped the outside-click and Escape-key auto-close paths (Escape still closes dropdowns). Hamburger is now the sole control that changes sidebar state. Removed the now-unused `closeSidebars()` method and the dead `body.sidebar-open` class bookkeeping.
- **README.md rewrite** — fixed clone URL (`markc/dcs` → `markc/dcs.spa`), dropped fragile per-file line counts, pointed the Documentation-site example at `themes/stone.css`, expanded the Components section to describe autonomous sidebars + panel carousel + Appearance panel + tree widget + width sliders, added links to the in-tree `_doc/` files.
- **index.html cleanup** — fixed 7 `markc/dcs` URLs, dropped stale line counts (705/245) from the architecture service cards and footer, updated App Shell card to reflect new features.
- **Mono scheme (6th)** — added `.scheme-mono` light/dark blocks in `site.css` with `C=0` (pure grayscale). Status colors (`--success`/`--danger`/`--warning`) intentionally inherit from the colored defaults for usability. Added the scheme button to the Appearance panel, a showcase card ("Mono (C=0)") in the Color Schemes grid, bumped the section title to "6 Color Schemes", and updated docs (`_doc/color-system.md`, CLAUDE.md, README.md). Accent uses strong dark/light greys rather than mid-grey because luminance alone must carry the accent distinction without chroma.
- **`save` project skill** — created `.claude/skills/save/SKILL.md` to standardise the journal + commit + push workflow for this repo.

## Sidebar width range + hero light-mode tweak

- **Widen sidebar slider range** — sliders (`docs/index.html:163-168`) now `min=10 max=100 value=15`. Default `--sidebar-width-left` / `--sidebar-width-right` dropped from 30% → 15% in `docs/base.css:34-37`. Replaced the pixel clamp `clamp(260px, …, 75vw)` with `clamp(10%, …, 100%)` so small percentages aren't floor-clamped back up to 260px. Updated range comments in `docs/base.js:125,372`.
- **Lighter hero in light mode** — `.hero-bg::before` now uses `var(--hero-overlay, rgb(0 0 0 / 0.6))`; `html.light` sets `--hero-overlay: rgb(0 0 0 / 0.3)` and bumps `.hero-bg p` to solid `#fff`. The h1 was already `#fff`, so the perceived "lighter text" comes from the weaker overlay. `site.css:458-470`.

## Retroactive journal backfill

- Filled in the missing `_journal/` entries for all pre-convention days: `2026-02-24.md` (initial release + DCS rename + CNAME), `2026-02-28.md` (panel carousel backport + Appearance panel + Slide/Fade polish), `2026-03-03.md` (tree widget + doc viewer + `.nojekyll`). Reconstructed from `git log` bodies, each flagged at the top as retroactive so they can't be mistaken for real-time notes.

## Memory: 100% sidebar = full-page content panel (bonus insight)

- Mark flagged that the widened 10–100% slider range unlocks an unintended capability: a pinned sidebar at 100% width covers the viewport, and combined with the panel carousel each sidebar becomes a multi-page content stack — two independent decks (left + right), already swipe-navigated by chevrons/dots.
- Concrete application discussed: a howto / doc / slidedeck presentation — one panel per slide, carousel does the paging for free.
- Saved as project memory at `~/.claude/projects/-home-cosmix--gh-dcs-spa/memory/sidebar_full_width_insight.md` so future DCS sessions suggest full-width sidebars before inventing new components.

## Notes for next time

- The `save` skill won't be discoverable until Claude Code is restarted so the new skill gets indexed.
- Flag: in Mono, check the accent dark/light greys against the hero and particles — may want to soften if too stark.
- Resize-to-mobile still force-closes pinned sidebars under 1280px — left in place because it's viewport-driven, not user-action-driven. Revisit if that feels wrong.
