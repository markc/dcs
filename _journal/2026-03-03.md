# 2026-03-03

Retroactively reconstructed from git history — `_journal/` convention came later.

## Session summary

Added the tree widget + doc-viewer flow that turns the left sidebar's third panel into a live docs browser.

- **Tree widget + doc viewer** (`0b374d6`) — new reusable tree component in `base.css`/`base.js`: depth-indented nodes via `--tree-depth`, chevron rotation, collapse via animated `grid-template-rows`, state in localStorage. Docs panel (third carousel slot) renders the `docs/` file tree with clickable `.md` entries; opening one replaces the SPA content with rendered markdown and exposes a back-to-showcase restore. `md.js` gained a `data-md-auto` guard and the prose wrapper. Four `_doc/` markdown files provide the real documentation content that the viewer reads.
- **Tree icon cleanup + `.nojekyll`** (`4bca649`) — dropped the chevron-down icons on tree toggles; the folder ↔ folder-open swap is a sufficient visual cue. Added `.nojekyll` so GitHub Pages serves the underscore-prefixed `_doc/` directory (Jekyll would otherwise skip it). Removed the now-unused `.tree-chevron` CSS.
- **Doc-viewer readable background** (`a058608`) — wrapped the doc viewer in a full-height `div` with `--bg-primary` background so rendered markdown is readable over the hero background image instead of fighting it.

## Notes

- `.nojekyll` is the key gotcha: without it, any folder starting with `_` is silently dropped by Pages. Worth remembering for every Pages site that uses the underscore-prefix convention for special-use folders.
